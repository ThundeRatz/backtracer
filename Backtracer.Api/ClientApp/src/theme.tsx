import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#011749",
    },
    secondary: {
      main: "#fce702",
    },
  },
});

export default theme;
